app:
  name: "Docker Swarm Demo Application"
  version: "1.0.0"
  environment: "production"

frontend:
  title: "🐳 Docker Swarm Demo - Frontend"
  refresh_interval: 30

backend:
  service_name: "backend"
  message: "Hello from Docker Swarm Backend! Load balancing is working perfectly!"
  api_version: "v1"
